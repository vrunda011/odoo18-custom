<?xml version="1.0" encoding="UTF-8" ?>

<odoo>
    <record id="view_res_doctor_form" model="ir.ui.view">
        <field name="name">res.doctor.form</field>
        <field name="model">res.doctor</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="specialization"/>
                        <field name="license_no"/>
                        <field name="experience_years"/>
                        <field name="hospital_id"/>
                        <field name="is_emergency_available"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_res_doctor_list" model="ir.ui.view">
        <field name="name">res.doctor.list</field>
        <field name="model">res.doctor</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="specialization"/>
                <field name="experience_years"/>
                <field name="hospital_id"/>
                <field name="is_emergency_available"/>
            </list>
        </field>
    </record>

    <menuitem id="menu_doctor_root" name="Doctors" parent="menu_bista_hms"/>

    <record id="action_doctor" model="ir.actions.act_window">
        <field name="name">Doctors</field>
        <field name="res_model">res.doctor</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_doctor_list" name="Doctors" parent="menu_doctor_root" action="action_doctor"/>
</odoo>