<?xml version="1.0" encoding="UTF-8" ?>

<odoo>
    <record id="action_custom_sale_order_report" model="ir.actions.report">
        <field name="name">Sale Order</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">custom_sale.custom_sale_order_template</field>
        <field name="report_file">custom_sale.custom_sale_order_template</field>
        <field name="print_report_name">('Order - %s' % (object.name))</field>
        <field name="binding_model_id" ref="model_sale_order"/>
        <field name="binding_type">report</field>
    </record>
</odoo>