<?xml version="1.0" encoding="utf-8" ?>

<odoo>
    <record id="view_expense_line_form" model="ir.ui.view">
        <field name="name">expense.line.form</field>
        <field name="model">expense.line</field>
        <field name="arch" type="xml">
            <form string="Expense Line">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="amount"/>
                        <field name="category_id"/>
                        <field name="expense_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="expense_line_action" model="ir.actions.act_window">
        <field name="name">Expense Lines</field>
        <field name="res_model">expense.line</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="expense_line_menu" name="Expense Lines" parent="expense_root_menu" action="expense_line_action"/>
</odoo>